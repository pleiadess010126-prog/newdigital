// This file was generated by Prisma, and assumes you have installed the following:
// npm install --save-dev prisma dotenv
import { config } from "dotenv";
import { defineConfig } from "prisma/config";

// Load .env.local first (for local overrides), then fall back to .env
config({ path: '.env.local' });
config({ path: '.env' });

// Fallback DATABASE_URL for local development if not set
const databaseUrl = process.env["DATABASE_URL"] || "postgresql://postgres:password@localhost:5432/digitalmeng?schema=public";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  datasource: {
    url: databaseUrl,
  },
});
